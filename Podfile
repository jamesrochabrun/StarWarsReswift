# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ReSwiftStarWars' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReSwiftStarWars
	pod "PromiseKit", "~> 4.0"
	pod 'ReSwift'


    target 'ReSwiftStarWarsTests' do
        use_frameworks!
        pod "Nimble"
        pod "Quick"
    end
end
